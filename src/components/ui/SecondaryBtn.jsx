const SecondaryBtn = ({ children }) => {
  return (
    <button className=" btn bg-white border rounded-lg border-orange-500 font-semibold">
      {children}
    </button>
  );
};

export default SecondaryBtn;
